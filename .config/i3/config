# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:DejaVu Sans Mono 45
font pango:DejaVu Sans Mono, Terminus Bold Semi-Condensed 45

# window title
font pango:Terminus 15px


set $mod Mod4

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+Q kill

# start rofi (a program launcher)
bindsym $mod+d exec rofi -show combi

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ntilde focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+J move left
bindsym $mod+Shift+K move down
bindsym $mod+Shift+L move up
bindsym $mod+Shift+Ntilde move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move workspace 1; workspace 1
bindsym $mod+Shift+2 move workspace 2; workspace 2
bindsym $mod+Shift+3 move workspace 3; workspace 3
bindsym $mod+Shift+4 move workspace 4; workspace 4
bindsym $mod+Shift+5 move workspace 5; workspace 5
bindsym $mod+Shift+6 move workspace 6; workspace 6
bindsym $mod+Shift+7 move workspace 7; workspace 7
bindsym $mod+Shift+8 move workspace 8; workspace 8
bindsym $mod+Shift+9 move workspace 9; workspace 9

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exec --no-startup-id i3-nagbar -t warning -m "Do you really want to exit i3?" -b "Yes" "i3-msg exit"

bindsym $mod+r mode "resize"

mode "App launch" {
	bindsym p exec pcmanfm; mode "default"
	bindsym c exec chrome; mode "default"
	bindsym s exec spotify; mode "default"
	bindsym t exec terminator; mode "default"
	bindsym w exec runorraise.sh instance "web.whatsapp.com" "google-chrome --user-data-dir=/home/chinomng/.config/google-chrome/Default/ --app=https://web.whatsapp.com/"; mode "default"
	bindsym Return mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+a mode "App launch"

# Custom config
bindsym control+mod1+l exec i3lock -c 000000
bindsym Print exec flameshot gui

# sound keys
bindsym XF86AudioRaiseVolume exec amixer -q -c 1 set Master 2dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q -c 1 set Master 2dB- unmute
bindsym XF86AudioMute exec amixer -q -c 1 set Master toggle

# brightness keys
bindsym XF86MonBrightnessUp exec echo $((`cat /sys/class/backlight/intel_backlight/brightness` - 10)) | sudo tee /sys/class/backlight/intel_backlight/brightness 
bindsym XF86MonBrightnessDown exec echo $((`cat /sys/class/backlight/intel_backlight/brightness` - 10)) | sudo tee /sys/class/backlight/intel_backlight/brightness

#bindsym XF86MonBrightnessUp exec --no-startup-id brightlight -i 20
#bindsym XF86MonBrightnessDown exec --no-startup-id brightlight -d 20

# move workspace between outputs
bindsym $mod+y move workspace to output left

# switch between the current and the previously focused one
bindsym $mod+b workspace back_and_forth
bindsym Mod1+Tab workspace back_and_forth
bindsym $mod+Shift+b move windows to workspace back_and_forth #not working

# bindsym Mod1+Tab workspace next_on_output
# bindsym Mod1+Shift+Tab workspace prev_on_output

bindsym $mod+Shift+z exec systemctl suspend

bindsym Mod1+q exec xrandr --output HDMI-2 --off
bindsym Mod1+w exec xrandr --output HDMI-1-1 --auto --mode 1920x1080 --left-of eDP-1 --output eDP-1 --auto --mode 1360x768
bindsym Mod1+e exec xrandr --output HDMI-2 --auto --mode 1920x1080 --output eDP-1 --off

# fast focus
bindsym $mod+F1  exec /home/chinomng/bash_dot/bin/runorraise.sh instance "gmail.com" "/usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-shiphero/ --class=shiphero --app=https://gmail.com"
bindsym $mod+F2  exec runorraise.sh instance "web.whatsapp.com" "/usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-chinomng/ --app=https://web.whatsapp.com/"
bindsym $mod+F3  exec runorraise.sh instance "translate.google.com" "/usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-chinomng/ --app=https://translate.google.com/"
bindsym $mod+F4  exec runorraise.sh instance "inbox.google.com" "/usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-chinomng/ --app=https://inbox.google.com/"
bindsym $mod+F5  exec runorraise.sh instance "www.youtube.com" "/usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-chinomng/ --app=https://www.youtube.com/"
bindsym $mod+F6  exec runorraise.sh instance "pcmanfm" pcmanfm
bindsym $mod+F8 exec "python /home/chinomng/bash_dot/scripts/pylauncher.py"


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the window is resized so that it has
        # more space on its left

        bindsym j resize shrink left 10 px or 10 ppt
        bindsym Shift+J resize grow   left 10 px or 10 ppt

        bindsym k resize shrink down 10 px or 10 ppt
        bindsym Shift+K resize grow   down 10 px or 10 ppt

        bindsym l resize shrink up 10 px or 10 ppt
        bindsym Shift+L resize grow   up 10 px or 10 ppt

        bindsym ntilde resize shrink right 10 px or 10 ppt
        bindsym Shift+Ntilde resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize grow width 1 px or 1 ppt
        bindsym Down resize shrink height 1 px or 1 ppt
        bindsym Up resize grow height 1 px or 1 ppt
        bindsym Right resize shrink width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
        font pango:Terminus 12
}

# load layouts. eDP1 is the laptop, HDMI-1-0 is the external monitor
workspace_layout tabbed

workspace 1 output HDMI-1-0
workspace 2 output HDMI-1-0
workspace 3 output HDMI-1-0
workspace 4 output HDMI-1-0
workspace 5 output HDMI-1-0
workspace 6 output HDMI-1-0
workspace 7 output HDMI-1-0
workspace 8 output HDMI-1-0
workspace 9 output HDMI-1-0

for_window [class="Tk"] floating enable focus

for_window [instance="terminator"] move to workspace 1
for_window [instance="x-terminal-emulator"] move to workspace 1
for_window [instance="shiphero.slack.com"] move to workspace 2
for_window [instance="google-chrome.*Profile.1"] move to workspace 3
for_window [instance="google-chrome.*Profile.2"] move to workspace 3
for_window [class="^shiphero$"] move to workspace 3
for_window [class="^jetbrains-pycharm$"] move to workspace 4
for_window [class="^jetbrains-pycharm-ce$"] move to workspace 4
for_window [class="^Gedit$"] move to workspace 5
for_window [class="^Pcmanfm$"] move to workspace 8
for_window [class="^chinomng$"] move to workspace 6
for_window [class="^Postman$"] move to workspace 7

for_window [instance="gmail.com"] move to workspace 8
for_window [instance="inbox.google.com"] move to workspace 8
for_window [instance="translate.google.com"] move to workspace 8
for_window [instance="web.whatsapp.com"] move to workspace 8
for_window [instance="www.youtube.com"] move to workspace 8

exec --no-startup-id sh -c 'source ~/.profile; exec i3'

# monitors configuration
exec --no-startup-id xrandr --output HDMI-1-0 --auto --mode 1920x1080 --left-of eDP-1 --output eDP-1 --auto --mode 1368x768

exec --no-startup-id xset m 1 1

# applets
exec --no-startup-id nm-applet # network
exec --no-startup-id gnome-sound-applet # sound
exec --no-startup-id copyq
exec --no-startup-id blueman-applet

# Apps to be opened on start up
exec --no-startup-id i3-msg 'exec terminator -l docker_monorepo'
exec --no-startup-id i3-msg 'exec /usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-shiphero/  --class=shiphero'
exec --no-startup-id i3-msg 'exec /snap/bin/slack'
exec --no-startup-id i3-msg 'exec /home/chinomng/Downloads/pycharm-2024.3.1.1/bin/pycharm'
exec --no-startup-id i3-msg 'exec gedit'
exec --no-startup-id i3-msg 'exec /usr/bin/brave-browser --user-data-dir=/home/chinomng/.config/BraveSoftware/profile-chinomng/  --class=chinomng'
exec --no-startup-id i3-msg 'exec systemctl --user start docker-desktop'
exec --no-startup-id numlockx on

# auto lock the screen
exec "xautolock -detectsleep -time 5 -locker \"i3lock -c 000000\""
exec --no-startup-id xss-lock -- i3lock -c 000000

exec --no-startup-id i3-msg 'workspace 10; exec terminator -e htop'

exec --no-startup-id dunst
